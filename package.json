{
  "name": "diagram-starter-vanilla",
  "version": "1.0.0",
  "description": "A simple web server to run the diagram starter.",
  "main": "server.js",
  "scripts": {
    "start": "start http://localhost:3000 && nodemon -e js,html server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "open": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "reload": "^3.4.2"
  }
}
